// Generated from packages/schema/schemas by `pnpm gen:schema`. Do not edit.
import { z } from "zod"

export const agentActionSchema = z.union([z.object({ "kind": z.literal("schedule_form"), "widgetId": z.string(), "berkasId": z.string().uuid(), "date": z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")), "time": z.string().regex(new RegExp("^\\d{2}:\\d{2}$")), "scheduleKind": z.enum(["pertemuan_klien","penandatanganan","internal"]), "location": z.string().max(200).optional(), "title": z.string().min(2).max(200) }).strict(), z.object({ "kind": z.literal("checklist_form"), "widgetId": z.string(), "berkasId": z.string().uuid(), "title": z.string().min(2).max(200), "dueDate": z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")).optional() }).strict(), z.object({ "kind": z.literal("berkas_picker"), "widgetId": z.string(), "berkasId": z.string().uuid(), "then": z.enum(["schedule","summary","kelengkapan","readiness"]), "text": z.string().max(2000) }).strict(), z.object({ "kind": z.literal("checklist_batch"), "widgetId": z.string(), "berkasId": z.string().uuid(), "items": z.array(z.object({ "title": z.string().min(2).max(200), "dueDate": z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")).optional() }).strict()).min(1).max(20) }).strict()]).describe("The user's answer to a WidgetEvent, sent with the next message. Handled deterministically, never by a model.")
export type AgentActionSchema = z.infer<typeof agentActionSchema>
